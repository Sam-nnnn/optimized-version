[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13, <4.0"
dependencies = [
    "alembic>=1.18.0",
    "asyncpg>=0.31.0",
    "fastapi>=0.128.0,<0.137",
    "geoalchemy2>=0.18.1",
    "google-auth>=2.0.0",
    "greenlet>=3.3.1",
    "h3>=4.4.1",
    "httpx>=0.28.1",
    "line-bot-sdk>=3.21.0",
    "phonenumbers>=8.13",
    "pydantic-settings>=2.12.0",
    "python-jose[cryptography]>=3.5.0",
    "python-multipart>=0.0.21",
    "redis[asyncio]>=5.0.0",
    "shapely>=2.1.2",
    "sqlalchemy>=2.0.45",
    "strawberry-graphql[fastapi]>=0.288.2",
    "uvicorn>=0.40.0",
    "fastapi-limiter>=0.2.0",
    "pyrate-limiter>=4.1.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "pytest-asyncio>=1.3.0",
    "ruff>=0.11.0",
]

[tool.hatch.build.targets.wheel]
packages = ["app"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
line-length = 110

[tool.ruff.lint]
extend-select = [
    "E501",   # line too long
    "C901",   # mccabe complexity
    "I",      # isort (import sorting)
    "UP",     # pyupgrade (modern syntax)
    "B",      # bugbear (common bugs)
    "SIM",    # simplify (redundant code)
    "ASYNC",  # async anti-patterns
    "D",      # pydocstyle (docstrings)
    "FAST",   # FastAPI-specific rules
]
ignore = [
    "B008",    # FastAPI Depends() in defaults is intentional
    "FAST002", # Old-style Depends is valid, Annotated migration is optional
    "D415",    # Chinese 。 is valid terminal punctuation; ASCII-only rule is too restrictive
    "UP046",   # PEP 695 generics are a style preference; Generic[T] syntax is still valid
]

[tool.ruff.lint.per-file-ignores]
"alembic/**" = ["D", "E501"]  # auto-generated migration files

[tool.ruff.lint.isort]
# Classify app/tests as first-party regardless of cwd, so import grouping is identical
# whether ruff runs from Backend/ (local) or repo root (CI). Without this, I001 import
# errors appear only from the repo root. Do not remove.
known-first-party = ["app", "tests"]

[tool.ruff.lint.pydocstyle]
convention = "google"

